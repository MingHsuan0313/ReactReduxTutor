import React from "react"

const Header = () =>{
    // this component unlike react.component class it can't has it's own class
    return(
        <div>
            Hello I am from stateless component header
        </div>
    )
}
